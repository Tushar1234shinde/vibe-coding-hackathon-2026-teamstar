# Qdrant Setup

This folder contains the vector search configuration for the platform.

- Qdrant is used to power semantic search for problems, solutions, research, and contributor matching.
- The backend includes `QdrantService` to manage collections and index documents.

Use the `QDRANT_URL` environment variable to connect to the local or managed Qdrant instance.
